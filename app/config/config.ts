// Maximum size limit for incoming request bodies
export const REQUEST_SIZE_LIMIT = '10mb';

// Maximum number of requests allowed per IP
export const REQUEST_LIMIT = 50;

// Time window (in milliseconds) for the request limit
export const REQUEST_LIMIT_TIME = 2000; //ms

// This flag indicates whether file logging is enabled to log every request in a file.
export const IS_REQUEST_LOG_ENABLED = true;
